{% extends 'base_back.html.twig' %}

{% block title %}{{ 'tickets.create_ticket'|trans }} - EcoSpot{% endblock %}
{% block page_title %}{{ 'tickets.create_ticket'|trans }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <p class="text-muted mb-3">{{ 'tickets.create_desc'|trans|raw }}</p>
            {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_row(form.title) }}
            {{ form_row(form.description) }}
            {{ form_row(form.imageFile) }}
            <div class="mb-3">
                <label class="form-label">{{ 'tickets.location_label'|trans }} <span class="text-danger">*</span></label>
                <div id="ticket-location-search-wrap" class="mb-2 position-relative">
                    <div class="input-group">
                        <input type="text" id="ticket-place-search" class="form-control" placeholder="{{ 'form.search_place_placeholder'|trans }}" autocomplete="off">
                        <button type="button" class="btn btn-outline-primary" id="ticket-location-search-btn">{{ 'common.search'|trans }}</button>
                    </div>
                    <div id="ticket-geocode-results" class="list-group mt-1 position-absolute start-0 w-100" style="z-index: 1050; display: none; max-height: 220px; overflow-y: auto; background: #fff;"></div>
                </div>
                <div id="ticket-location-selected" class="d-none">
                    <div class="input-group">
                        <input type="text" id="ticket_location_display" class="form-control bg-light" readonly placeholder="{{ 'form.no_location'|trans }}">
                        <button type="button" class="btn btn-outline-secondary" id="ticket-location-clear">{{ 'common.change_location'|trans }}</button>
                    </div>
                </div>
                <div class="form-text">{{ 'tickets.location_help'|trans }}</div>
                {{ form_widget(form.location, { attr: { class: 'd-none' } }) }}
                {{ form_widget(form.latitude) }}
                {{ form_widget(form.longitude) }}
                {{ form_errors(form.location) }}
            </div>
            {{ form_row(form.priority) }}
            {{ form_row(form.domain) }}
            <div class="d-flex gap-2 mt-3 flex-wrap">
                <button type="submit" class="btn btn-primary">{{ 'tickets.submit_ticket'|trans }}</button>
                <a href="{{ path('ticket_my_list') }}" class="btn btn-outline-secondary">{{ 'common.cancel'|trans }}</a>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
    document.addEventListener('DOMContentLoaded', function() {
        const locationInput = document.querySelector('#ticket_location');
        const locationDisplay = document.getElementById('ticket_location_display');
        const latInput = document.querySelector('#ticket_latitude');
        const lngInput = document.querySelector('#ticket_longitude');
        const searchInput = document.getElementById('ticket-place-search');
        const searchWrap = document.getElementById('ticket-location-search-wrap');
        const selectedWrap = document.getElementById('ticket-location-selected');
        const searchBtn = document.getElementById('ticket-location-search-btn');
        const clearBtn = document.getElementById('ticket-location-clear');
        const resultsEl = document.getElementById('ticket-geocode-results');

        function doSearch() {
            const q = searchInput && searchInput.value ? searchInput.value.trim() : '';
            if (q.length < 2) { resultsEl.style.display = 'none'; return; }
            resultsEl.innerHTML = '<div class="list-group-item">{{ 'tickets.searching'|trans }}</div>';
            resultsEl.style.display = 'block';
            const url = '{{ path('api_geocode') }}?q=' + encodeURIComponent(q);
            fetch(url, { credentials: 'same-origin' })
                .then(function(r) {
                    if (!r.ok) throw new Error('API returned ' + r.status);
                    return r.json();
                })
                .then(function(data) {
                    if (!Array.isArray(data)) { resultsEl.innerHTML = '<div class="list-group-item text-danger">{{ 'tickets.invalid_response'|trans }}</div>'; return; }
                    if (!data.length) { resultsEl.innerHTML = '<div class="list-group-item">{{ 'tickets.no_results'|trans }}</div>'; return; }
                    resultsEl.innerHTML = data.map(function(r) {
                        var name = (r.display_name || '').replace(/"/g, '&quot;').replace(/</g, '&lt;');
                        return '<a href="#" class="list-group-item list-group-item-action" data-name="' + name + '" data-lat="' + (r.lat || '') + '" data-lon="' + (r.lon || '') + '">' + (r.display_name || '') + '</a>';
                    }).join('');
                })
                .catch(function(err) {
                    resultsEl.innerHTML = '<div class="list-group-item text-danger">{{ 'tickets.search_failed'|trans }}: ' + (err.message || 'check connection') + '. {{ 'common.try_again'|trans }}.</div>';
                });
        }

        function selectPlace(name, lat, lon) {
            if (locationInput) locationInput.value = name || '';
            if (locationDisplay) locationDisplay.value = name || '';
            if (latInput) latInput.value = lat || '';
            if (lngInput) lngInput.value = lon || '';
            resultsEl.style.display = 'none';
            searchWrap.classList.add('d-none');
            selectedWrap.classList.remove('d-none');
        }

        function clearLocation() {
            if (locationInput) locationInput.value = '';
            if (locationDisplay) locationDisplay.value = '';
            if (latInput) latInput.value = '';
            if (lngInput) lngInput.value = '';
            searchWrap.classList.remove('d-none');
            selectedWrap.classList.add('d-none');
            if (searchInput) searchInput.value = '';
        }

        if (searchBtn) searchBtn.addEventListener('click', doSearch);
        if (searchInput) searchInput.addEventListener('keydown', function(e) { if (e.key === 'Enter') { e.preventDefault(); doSearch(); } });
        if (clearBtn) clearBtn.addEventListener('click', clearLocation);

        resultsEl.addEventListener('click', function(e) {
            e.preventDefault();
            var a = e.target.closest('a[data-lat]');
            if (!a) return;
            selectPlace(a.getAttribute('data-name'), a.getAttribute('data-lat'), a.getAttribute('data-lon'));
        });
    });
    </script>
{% endblock %}
