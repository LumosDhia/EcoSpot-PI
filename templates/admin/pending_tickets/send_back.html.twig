{% extends 'base_back.html.twig' %}

{% block title %}{{ 'admin.send_back_ticket'|trans }} - EcoSpot Admin{% endblock %}
{% block page_title %}{{ 'admin.send_back_ticket'|trans }}{% endblock %}

{% block body %}
    <div class="card mb-3">
        <div class="card-body">
            <h5 class="card-title">{{ ticket.title|translate }}</h5>
            <p class="card-text text-muted">{{ ticket.description|translate|slice(0, 200) }}{% if ticket.description|length > 200 %}...{% endif %}</p>
            <p class="small mb-0">{{ 'common.by'|default('By')|translate }} {{ ticket.user.firstname }} {{ ticket.user.lastname }} ({{ ticket.user.email }})</p>
        </div>
    </div>
    <div class="card">
        <div class="card-body">
            <form method="post" action="{{ path('admin_pending_ticket_send_back', { id: ticket.id }) }}">
                <input type="hidden" name="_token" value="{{ csrf_token('pending-ticket-sendback-' ~ ticket.id) }}">
                <div class="mb-3">
                    <label for="note" class="form-label">{{ 'admin.note_for_user_label'|trans }} <span class="text-danger">*</span></label>
                    <textarea name="note" id="note" class="form-control" rows="4" required placeholder="{{ 'admin.send_back_placeholder'|trans }}"></textarea>
                    <div class="form-text">{{ 'admin.send_back_desc'|trans }}</div>
                </div>
                <div class="d-flex gap-2 flex-wrap">
                    <button type="submit" class="btn btn-warning">{{ 'common.return_revision'|trans }}</button>
                    <a href="{{ path('admin_pending_tickets') }}" class="btn btn-outline-secondary">{{ 'common.cancel'|trans }}</a>
                </div>
            </form>
        </div>
    </div>
{% endblock %}
